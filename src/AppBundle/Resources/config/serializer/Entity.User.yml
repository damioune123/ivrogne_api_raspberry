AppBundle\Entity\User:
    exclusion_policy:

    properties:
        id:
            groups: ['user', 'userAccount', 'order', 'auth-token','moneyFlow', 'userLimited']

        firstname:
            groups: ['user',  'userLimited','order']

        lastname:
            groups: ['user','userLimited','order']

        promotion:
            groups: ['user','userLimited','order']

        username:
            groups: ['user','userLimited']

        godfather:
            groups: ['user']

        nefews:
            groups: ['user']

        email:
            groups: ['user']

        role:
            groups: ['user','userLimited']

        userAccounts:
            groups: ['user','userLimited']

        plainPassword:
            exclude: true

        isRemoved:
            groups: ['user','userLimited']


