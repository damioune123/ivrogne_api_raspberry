AppBundle\Entity\UserAccount:
    exclusion_policy: NONE
    properties:
        id:
            groups: ['user','moneyFlow','auth-token','userAccount', 'account','userLimited','order','userLimited']

        type:
            groups: ['userAccount', 'user', 'account']

        moneyBalance:
            groups: ['userAccount','user', 'account','userLimited']

        user:
            groups: ['userAccount','order','moneyFlow']

        orders:
            groups: []

        registerOrders:
            groups: []

        positiveMoneyFlows:
            groups: []

        negativeMoneyFlows:
            groups: []

        moneyLimit:
                groups: ['user','account','userAccount','userLimited']

        creditAllowed:
              groups: ['user','account','userAccount']


    virtual_properties:

        getAvailableBalance:
            groups: ['user','account','userAccount','userLimited']


